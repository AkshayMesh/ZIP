import { useColorScheme } from "react-native"

// const isDarkMode = useColorScheme() === 'dark';
const MyTheme = {
    primary: "#1977fa",
    secondary: "#87458a",
    windowBackground: 'white',
    statusBarColor: 'white',
    link: '#619ffa'
}
// const isDarkMode = useColorScheme() === 'dark';
const MyDarkTheme = {
    primary: "#bef702",
    secondary: "#FF6347",
    windowBackground: 'black',
    statusBarColor: 'black'
}
 export {MyDarkTheme, MyTheme}